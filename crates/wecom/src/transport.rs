//! Re-exports of [`wecom_transport`] public API.
//!
//! This module provides a single namespace (`wecom::transport::*`) for all
//! transport-layer types so that downstream crates never need to depend on
//! `wecom-transport` directly.

pub use wecom_transport::*;
