[package]
name = "wecom-error"
version = "0.1.0"
edition = "2024"
description = "Unified capability contract for all error types across the workspace."
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
serde_json = { workspace = true }
